fastapi==0.95.0
uvicorn[standard]==0.22.0
pandas==1.5.0
numpy==1.21.0
matplotlib==3.6.0
python-multipart==0.0.5
twelvelabs==0.1.0   # Replace with the correct version if available, or leave without a version specifier if necessary.
gunicorn==20.1.0
